{
  "name": "@wave-av/dispatch",
  "version": "0.7.0",
  "description": "wave Dispatch — route each request to the cheapest capable model (local-first; escalate to your frontier model only when needed). BYO keys + infra; we return the routing decision.",
  "type": "module",
  "main": "index.js",
  "types": "index.d.ts",
  "exports": { ".": { "types": "./index.d.ts", "default": "./index.js" } },
  "bin": { "dispatch": "bin/dispatch.js" },
  "files": ["index.js", "index.d.ts", "bin/", "README.md"],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  },
  "keywords": ["llm", "routing", "local-first", "cost", "edge", "wave", "dispatch", "x402", "agents"],
  "author": "WAVE Online, LLC",
  "license": "MIT",
  "homepage": "https://dispatch.wave.online",
  "repository": { "type": "git", "url": "git+https://github.com/wave-av/dispatch-edge.git" },
  "engines": { "node": ">=18" },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
